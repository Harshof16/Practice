var name = `harsh's`;  //the benefit of string literal(backtick) is that we can add quotes without escaping
var fullName = `${name} shukla`;

console.log(fullName);
console.log(fullName.charAt[0]);
console.log(fullName.endsWith("!!!"));
console.log(fullName.toUpperCase());
